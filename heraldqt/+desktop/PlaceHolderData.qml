import QtQuick 2.0

ListModel {
               ListElement
               {
                   name: "gooba"
                   colorCode : "#f0f0f0"
               }

               ListElement {
                   name: "Red"
                   colorCode: "red"
               }

               ListElement {
                   name: "Blue"
                   colorCode: "blue"
               }

               ListElement {
                   name: "Green"
                   colorCode: "green"
               }
  }
