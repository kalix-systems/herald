INSERT INTO
  messages(
    msg_id,
    author,
    conversation_id,
    body,
    send_status,
    has_attachments,
    insertion_ts,
    server_ts,
    expiration_ts,
    known
  )
VALUES(
  @msg_id,
  @author,
  @conversation_id,
  @body,
  @send_status,
  @has_attachments,
  @insertion_ts,
  @server_ts,
  @expiration_ts,
  1
)
