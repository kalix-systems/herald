INSERT INTO
  conversations(
    conversation_id,
    title,
    picture,
    color,
    pairwise,
    muted,
    last_active_ts,
    expiration_period
  )
VALUES(
  @conversation_id,
  @title,
  @picture,
  @color,
  @pairwise,
  @muted,
  @last_active_ts,
  @expiration_period
)
